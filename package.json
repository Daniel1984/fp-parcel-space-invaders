{
  "name": "fp-space-invaders",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "parcel index.html -p 8081",
    "build": "rimraf dist && NODE_ENV=production parcel build index.html"
  },
  "devDependencies": {
    "node-sass": "^4.8.3",
    "parcel-bundler": "^1.7.1",
    "rimraf": "^2.6.2"
  },
  "dependencies": {
    "autoprefixer": "^8.3.0",
    "babel-preset-env": "^1.6.1",
    "postcss-modules": "^1.1.0",
    "reset-css": "^3.0.0",
    "rxjs": "^5.5.10"
  }
}
